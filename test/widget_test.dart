import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add widget test once app is configured
    expect(1 + 1, equals(2));
  });
}
